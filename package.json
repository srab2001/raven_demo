{
  "name": "raven-squares-demos",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "demo1-lighthouse-under-load/apps/web",
    "demo2-508-eligibility-wizard",
    "demo3-vendor-rigor-console/apps/web"
  ],
  "scripts": {
    "build": "rm -rf dist && npm run build --workspaces && mkdir -p dist && cp site/index.html dist/index.html && cp -R demo1-lighthouse-under-load/apps/web/dist dist/demo1 && cp -R demo2-508-eligibility-wizard/dist dist/demo2 && cp -R demo3-vendor-rigor-console/apps/web/dist dist/demo3"
  }
}